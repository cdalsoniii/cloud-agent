# direnv: export OPENROUTER_API_KEY when you cd into cloud-agent (optional).
# Install: brew install direnv && hook direnv into your shell.
if [ -f .env ]; then
  set -a
  # shellcheck disable=SC1091
  source .env
  set +a
fi
