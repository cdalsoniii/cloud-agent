import type { Mastra } from '@mastra/core';
import type { Workflow } from '@mastra/core/workflows';

// Stub workflow to avoid import errors during compilation
export const verifyMcpWorkflow = {} as unknown as Workflow;
