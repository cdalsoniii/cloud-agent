# Lean 4 (optional)

Stub directory for deep proofs (token isolation, validation-gate theorems).

Dafny specs under `../dafny/` cover the same properties with a lighter local toolchain.

When adding Lean proofs, add a `lakefile.lean` + `lean-toolchain` and extend `verify-local.sh`.
